import styled from 'styled-components';

export const Title = styled.h1`
	color: #222;
	font-size: 2.5rem;
`;

export const BlogPostTitle = styled.h2`
	color: #08a08c;
	font-size: 1.75rem;
	font-weight: 700;
`;

export const Author = styled.span`
	font-size: 1rem;
	color: hotpink;
	font-weight: 500;
`;

export const Subtitle = styled.h4`
	font-size: 1.5rem;
	color: #b39200;
	font-weight: 600;
`;

export const Copy = styled.section`
	p {
		font-size: 1rem;
		font-weight: 400;
		color: #222;
		line-height: 1.5;
		font-family: Merriweather, Georgia, Cambria, 'Times New Roman', Times, serif;
	}
`;

export const FooterStles = styled.footer`
	font-family: Montserrat;
	font-size: 1.125rem;
	font-weight: 700;
	text-align: center;
	text-transform: uppercase;

	a {
		color: #0081ac;
		text-decoration: none;
	}
`;
